{
  "schema_version": 1,
  "comment": "Shipped JIT cache seeds. Each entry under 'entries' is keyed by 16-hex-char title id. Build seeds with tools/cache_seed/build_seed.py. If 'entries' is empty or a title has no matching entry, the runtime falls through to phase B precompile -- no failure, just slower first launch for that title.",
  "entries": {
  }
}
